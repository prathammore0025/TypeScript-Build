{
  "name": "demo-typescript-project",
  "version": "1.0.0",
  "description": "A simple demo TypeScript project",
  "main": "dist/index.js",  // Changed to 'dist' since you're using 'dist' for the build
  "scripts": {
    "clean": "rimraf ./dist",  // Add this clean script to remove the 'dist' directory
    "build": "npm run clean && tsc", // Run clean before compiling
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.0.1",  // rimraf is used to remove files and directories
    "typescript": "^5.6.2"
  }
}
